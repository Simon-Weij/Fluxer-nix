# Nix flake for fluxer

A nix flake that wraps the appImage in a nix flake so you can install it easily.

Will probably be archieved once fluxer gets packaged for nixpkgs, also the hash needs manual updating so if I forget it, feel free to open a PR
